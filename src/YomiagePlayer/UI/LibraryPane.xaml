<UserControl x:Class="YomiagePlayer.UI.LibraryPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:YomiagePlayer.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:LibraryViewModel}"
             MaxHeight="260">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2D2D30" Padding="10,8">
            <Grid>
                <TextBlock Text="ライブラリ" Foreground="#EEE" FontWeight="Bold" VerticalAlignment="Center" />
                <Button Content="＋" Width="26" Height="26" HorizontalAlignment="Right"
                        Background="Transparent" Foreground="#EEE" BorderThickness="0"
                        Cursor="Hand" ToolTip="フォルダを追加" Click="AddFolder_Click" />
            </Grid>
        </Border>

        <ListBox x:Name="List" Grid.Row="1"
                 ItemsSource="{Binding Folders}"
                 Background="Transparent" BorderThickness="0"
                 HorizontalContentAlignment="Stretch"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 MouseDoubleClick="List_MouseDoubleClick">
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="プレイリストへ展開(置換)" Click="OpenReplace_Click" />
                    <MenuItem Header="プレイリストへ追加" Click="OpenAppend_Click" />
                    <Separator />
                    <MenuItem Header="エクスプローラーで開く" Click="OpenInExplorer_Click" />
                    <Separator />
                    <MenuItem Header="ライブラリから削除" Click="RemoveFolder_Click" />
                </ContextMenu>
            </ListBox.ContextMenu>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="2">
                        <TextBlock Text="📁" Margin="0,0,6,0" />
                        <TextBlock Text="{Binding DisplayName}" Foreground="#CCC"
                                   TextTrimming="CharacterEllipsis" ToolTip="{Binding Path}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
